<script type="text/javascript">
  var discountable = '<%= @discountable %>'
</script>

<%= form_with(model: discount, local: true) do |form| %>
  <% if discount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discount.errors.count, "error") %> prohibited this discount from being saved:</h2>

      <ul>
      <% discount.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :entity_type %>
    <%= form.select :entity_type, ['Role', 'Inventory'] %>
  </div>

  <div class="field">
    <%= form.label :discount_field %>
    <%= form.select :discount_field, Role.all.collect { |u| [u.name, u.id] } %>
  </div>

  <div class="field">
    <%= form.label :discount_condition %>
    <%= form.select :discount_condition, [' ', '>', '<', 'equals'] %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value  %>
  </div>

  <div class="field">
    <%= form.label :discount_unit %>
    <%= form.select :discount_unit, ['Amount', 'Percentage']  %>
  </div>

  <div class="field">
    <%= form.label :discount_value %>
    <%= form.text_field :discount_value  %>
  </div>

  <div class="actions">
    <%= form.submit class: 'action-btn' %>
  </div>
<% end %>
